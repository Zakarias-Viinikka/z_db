[package]
name = "db"
version = "0.1.0"
edition = "2024"

[dependencies]
rusqlite = { version = "0.40.2", features = ["bundled-full"] }
protocol = { path = "../protocol" }
sql_builder = { path = "../sql_builder" }
